export function SiteFooter() {
  return (
    <footer className="border-t border-red-500/20 bg-black/80 backdrop-blur">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-8 text-sm text-zinc-400 sm:px-6 lg:px-8 md:flex-row md:items-center md:justify-between">
        <p>SquadLink. MVP académico listo para Vercel y Supabase.</p>
        <p>Compatibilidad explicable, RLS preparada y modo demo para defensa.</p>
      </div>
    </footer>
  );
}
